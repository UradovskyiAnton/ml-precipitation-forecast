# ml-precipitation-forecast
An interactive, end-to-end Machine Learning web application built with Streamlit that predicts daily precipitation (rain) using real meteorological data. This project implements a full ML pipeline, from automated data extraction via the Open-Meteo API to strict time-series cross-validation.
